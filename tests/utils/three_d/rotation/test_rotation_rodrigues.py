def test():
    # this test should randomly sample 10 pairs of rotation axes (unit vectors) and rotation angles from [0, 2pi)
    # for each sampled rotation angle, convert the axis-angle representation to a rotation matrix using axis_angle_to_matrix
    # then, convert this matrix immediately back to axis-angle representation using matrix_to_axis_angle
    # check that the axis and angle matches (almost) exactly the value you started from.
    return
